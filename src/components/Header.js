import React from 'react';

const Header = () => {
    return (
        <div className="header__background">
            <span className="header_text">Todo List</span>
        </div>
    );
};

export default Header;
